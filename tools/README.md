# GreenBoost — Tools

## Available tools

| Tool | CLI command | Purpose |
|------|-------------|---------|
| `gb_workstation_bench.py` | `greenboost benchmark` | T1/T2/T3 bandwidth benchmark — run via `greenboost benchmark` |

All other tools have been consolidated into the `greenboost` CLI.
Use `greenboost help` or `greenboost wizard` for the full command reference.
