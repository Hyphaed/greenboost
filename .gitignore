# Kernel module build artifacts
*.ko
*.o
*.mod
*.mod.c
# Go module files must not be caught by *.mod above
!go.mod
!go.sum
*.symvers
*.order
.*.cmd
.tmp_versions/
Module.symvers
modules.order

# Compiled shared library
libgreenboost_cuda.so

# Editor temp files
nano.*.save
*.swp
*.swo
*~

# Scratch / planning notes (local only)
*plan.md
plan*.md
apply_fixes.sh

# AI assistant local context (never commit)
.claude/
.claude-sessions/
.mcp-cache/
CLAUDE.md
MEMORY.md
memory/
*.claude.md
.claude_*
.continue/

# Internal development notes (kept local, not shipped)
skills_needed.md
AUDIT_*.md

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Python venv (managed by greenboost_setup.sh)
venv/
.venv/

# GitLab push helper (local-only, never commit credentials or remote config)
push_to_gitlab.sh
