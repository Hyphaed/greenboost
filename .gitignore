# Kernel module build artifacts
*.ko
*.o
*.mod
*.mod.c
# Go module files must not be caught by *.mod above
!go.mod
!go.sum
*.symvers
*.order
.*.cmd
.tmp_versions/
Module.symvers
modules.order

# Compiled shared library
libgreenboost_cuda.so

# Per-node build output — vendored llama.cpp is built locally on every
# cluster node (host Intel / feeder AMD), never committed.
third_party/llama.cpp/build-synapse/

# Editor temp files
nano.*.save
*.swp
*.swo
*~

# Scratch / planning notes (local only)
*plan.md
plan*.md
apply_fixes.sh

# Bench harness output (generated, machine-local results)
tests/bench/results.jsonl

# AI assistant local context (never commit)
.claude/
.claude-sessions/
.mcp-cache/
# .mcp.json IS tracked (owner decision 2026-07-13): it's the project-scope MCP
# registration for a fresh clone — 5 servers, relative python3 commands, no
# machine paths or secrets. CLAUDE.md/workflow/ stay untracked below.
CLAUDE.md
MEMORY.md
memory/
*.claude.md
.claude_*
.continue/

# Internal development notes (kept local, not shipped)
skills_needed.md
AUDIT_*.md

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage

# synapse_engine packaging build artifacts (regenerated by `pip install`)
synapse_engine/build/
synapse_engine/*.egg-info/

# Python venv (managed by greenboost_setup.sh)
venv/
.venv/

# GitLab push helper (local-only, never commit credentials or remote config)
push_to_gitlab.sh

# Claude working folder (session plans, agent docs) , never committed
workflow/
greenboost_next.md

# Runtime cache (index.db etc) , never expose
.cache/

# Local dev/test/deploy scripts , may carry tokens & local paths, never commit
deploy_and_test.py
deploy_shim.sh
run_test.sh
test_qwen_vllm.sh
test_cluster_qwen3.sh
watch_nvtx.sh

# All compiled shared libraries (libgreenboost_cuda.so already covered above)
*.so

# gb-synapse: HuggingFace tokens, pulled GGUF weights, built engine - never
# committed. Runtime state (/etc, /var/lib, /run) lives outside the repo tree
# by default; these patterns only guard against a dev pointing GB_SYNAPSE_*
# env overrides at an in-repo path during local testing.
hf_token
*.gguf
synapse_models/

# Compiler intermediates and manual editor backups
*.ii
proton.bak
proton_copy

# Stray / generated files that must not be committed
sudo
build_info
greenboost-ebpf-trace
*.deb

# Claude agent skills/working files , never committed
.agents/
enhancements.md
greenboost_next_check_polishroundup.md
greenboost_jul12_1.md


# Checks
checks/
docs/
AGENTS.md