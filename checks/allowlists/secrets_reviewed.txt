# Reviewed IP/home-path false positives — checks/check_secrets.py.
# Each entry: exact "path:line" (never a glob for this file — every entry
# here was individually read and judged illustrative, not a leak). Secret
# TOKEN matches are never allowlisted; only IP-shape and home-path-shape
# matches land here. Add a new entry only after reading the actual line.

# Doc usage examples (e.g. "sudo greenboost connect <lan-ip>") — illustrative
# CLI syntax, not a real feeder's address. (Kept out of literal digit form in
# this comment itself — checks/check_secrets.py scans this file too, and a
# literal 192.168.x.x string here would self-flag.)
DOCUMENTATION.md:1646
DOCUMENTATION.md:1649
DOCUMENTATION.md:1650
DOCUMENTATION.md:1656
README.md:440
docs/qwen36-35b-on-the-cluster.md:56

# Unit test fixtures using synthetic RFC 1918 addresses (LAN-filter /
# cluster-config parsing tests) — never resolve to real infrastructure.
greenboost-cli/tests/test_core.py:519
greenboost-cli/tests/test_core.py:522
tests/test_lan_filter.py:40
tests/test_gb_synapse_api_auth.py:71

# Synthetic single-letter test username "u" in a fake subprocess stdout
# fixture — not a real developer's home directory.
tests/test_gb_cluster.py:428
tests/test_gb_cluster.py:470

# ── 2026-08-19 pre-push review (first publication of this tree) ─────────────
# Every entry below was read line-by-line before being added. Anything that
# named the owner's REAL infrastructure was EDITED, not allowlisted: the
# feeder address formerly in test_gb_router.py and test_local_first.py, and
# the absolute home paths in handlers.py and tasks_10x.md.

# `sudo greenboost connect <lan-ip>` CLI usage examples — illustrative syntax
# in docs, not a real feeder. Same class as the DOCUMENTATION.md entries above;
# these are the newer examples added since that review.
DOCUMENTATION.md:1816
DOCUMENTATION.md:1819
DOCUMENTATION.md:1820
DOCUMENTATION.md:1826
README.md:502

# Synthetic RFC 1918 addresses in unit tests. test_local_first.py needs a
# 192.168.x.x literal specifically — it asserts that all three RFC 1918 blocks
# count as "local" under the Local-First rule, so substituting a TEST-NET
# address would silently delete that block's coverage.
tests/test_local_first.py:17
tests/test_gb_router.py:91

# Fake home paths under a one-letter username, used as process-cmdline
# fixtures in the reclaim-scope and factory-TTL tests. No such user exists;
# they are deliberately unlike any real developer home. (Spelled out in words
# rather than shown literally — check_secrets.py scans this file too, and the
# literal path form in a comment here self-flags, exactly as the header warns
# for the LAN-IP case.)
tests/test_gb_reclaim_inference_scope.py:55
tests/test_gb_reclaim_inference_scope.py:56
tests/test_gb_reclaim_inference_scope.py:57
tests/test_gb_reclaim_inference_scope.py:69
tests/test_gb_reclaim_inference_scope.py:91
tests/test_gb_reclaim_inference_scope.py:113
greenboost-cli/tests/test_factory_pending_ttl.py:69
greenboost-cli/tests/test_factory_pending_ttl.py:70
greenboost-cli/tests/test_factory_pending_ttl.py:73

# Vendored upstream llama.cpp documentation (--rpc usage example). Third-party
# file — never edited locally, per the vendoring policy.
third_party/llama.cpp/tools/rpc/README.md:80
