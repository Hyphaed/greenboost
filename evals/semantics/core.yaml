# evals/semantics/core.yaml — GB-Semantics eval set.
#
# Ground truth is pinned to tests/fixtures/semantics/events.py's fixture
# scenario (a single frozen snapshot of Fable-Fusion-27B serving on a 12 GB
# RTX 5070, mid-generation, with a deliberate Rule #1 violation baked in —
# see that file's docstring). tests/test_semantics_evals.py loads the
# fixture into an isolated dataflux log per test and runs every entry below
# against the REAL gb_semantics engine — no mocking of resolve()/
# evaluate_segment()/answer() themselves.
#
# `kind: metric` entries with `source: fixture` assert an EXACT value (the
# resolver reads only the injected fixture events, so the outcome is
# deterministic on any machine). `source: live` entries only assert the
# resolver runs without raising and returns a well-formed dict — those
# resolvers read the ACTUAL box's kernel module / free RAM / running
# servers, which legitimately differs machine to machine; check_semantics_
# coverage.py plus these structural checks jointly guard against a renamed/
# missing resolver, which is the regression class this suite exists to catch.
#
# `category: trap` entries additionally carry `trap_raw_field` — the field a
# naive, ungoverned answer would read instead, and `trap_raw_wrong: true`
# asserting that field's fixture value, read alone, gives the WRONG verdict.
# The test runner tallies these as GreenBoost's own before/after number, the
# local analogue of Anthropic's 21% -> 95%.

# ── metrics, fixture-exact ───────────────────────────────────────────────
- id: vram_fill_pct_reads_physical_not_virtual
  kind: metric
  name: vram_fill_pct
  source: fixture
  category: trap
  question: "Is Rule #1 (90% VRAM) satisfied right now?"
  expect: {value: 42.3, threshold_state: below_target}
  trap_raw_field: fb_used_pct
  trap_raw_value: 97.0
  trap_raw_wrong: true
  trap_why: >
    fb_used_pct=97.0 (virtual, shim-inflated) reads as "essentially full" /
    Rule #1 satisfied. The real physical figure is 42.3% — Rule #1 is being
    VIOLATED. Reading the raw field alone reports the opposite of the truth.

- id: t2_pressure_fraction_exact
  kind: metric
  name: t2_pressure_fraction
  source: fixture
  category: trap
  question: "What's the current T2 DDR pressure (continuous)?"
  expect: {value: 0.72, threshold_state: warn}
  trap_raw_field: t2_pressure_level_thresholds
  trap_raw_wrong: true
  trap_why: >
    0.72 read against the ENUM scale's thresholds (warn_at=1, critical_at=2)
    would misclassify as "nominal" (0.72 < 1) when the fraction scale's own
    thresholds (warn_at=0.6) correctly call this "warn". Same field name,
    two incompatible scales — the exact collision t2_pressure_level's
    never_use entry documents.

- id: kv_resident_share_exact
  kind: metric
  name: kv_resident_share
  source: fixture
  expect: {value: 0.3}
  question: "What share of KV cache is resident in VRAM vs spilled to T2?"

- id: effective_bits_exact
  kind: metric
  name: effective_bits
  source: fixture
  expect: {value: 8}
  question: "What bit-width is the served model actually running at?"

- id: meets_fp8_floor_true
  kind: metric
  name: meets_fp8_floor
  source: fixture
  expect: {value: true}
  question: "Does the currently-served model meet the fp8 quality floor?"

- id: kv_bits_by_layer_exact
  kind: metric
  name: kv_bits_by_layer
  source: fixture
  expect: {value_subset: {k_bits: 4, v_bits: 3}}
  question: "What K/V bit-widths is TurboQuant using?"

- id: tok_s_decode_exact
  kind: metric
  name: tok_s_decode
  source: fixture
  expect: {value: 5.2}
  question: "What's the measured decode speed right now?"

- id: ttft_ms_exact
  kind: metric
  name: ttft_ms
  source: fixture
  expect: {value: 4200.0}
  question: "What's the time-to-first-token for the current serve session?"

- id: engine_prefill_ms_exact
  kind: metric
  name: engine_prefill_ms
  source: fixture
  expect: {value: 1350.0}
  question: "How long did the engine itself spend on prefill?"

- id: prompt_depth_tokens_exact
  kind: metric
  name: prompt_depth_tokens
  source: fixture
  expect: {value: 1333}
  question: "How deep is the conversation the last sample was measured at?"

- id: prompt_cache_hit_pct_exact
  kind: metric
  name: prompt_cache_hit_pct
  source: fixture
  expect: {value: 3.0}
  question: "What's the prompt-cache hit rate right now?"

- id: slot_pin_rate_pct_from_window
  kind: metric
  name: slot_pin_rate_pct
  source: fixture
  # 1 of 2 events kept its slot ("pinned-edited" counts — the conversation DID
  # come back to its own slot, it just also changed a chunk).
  expect: {value: 50.0}
  question: "How often are conversations getting the slot that still holds their cache?"

- id: last_edit_chunk_names_the_chunk
  kind: metric
  name: last_edit_chunk
  source: fixture
  expect: {value: 2}
  question: "Which part of the conversation changed on the last turn?"

- id: feeder_items_exact
  kind: metric
  name: feeder_items
  source: fixture
  expect: {value_subset: {"feeder1 (192.0.2.10)": 0}}
  question: "How many items has the connected feeder actually processed?"

- id: link_bw_gbps_no_data
  kind: metric
  name: link_bw_gbps
  source: fixture
  expect: {value: null}
  question: "What's the measured host<->feeder link bandwidth?"

# ── metrics, live (structural: resolver runs, well-formed dict) ──────────
- id: t2_pressure_level_structural
  kind: metric
  name: t2_pressure_level
  source: live

- id: t3_spill_active_mb_structural
  kind: metric
  name: t3_spill_active_mb
  source: live

- id: t2_overflow_active_mb_structural
  kind: metric
  name: t2_overflow_active_mb
  source: live
  # Structural only: the resolver reads /run/greenboost/shim_stats directly, so
  # the dataflux fixture harness cannot stage a value for it. The pressure-vs-
  # overflow trap this metric exists to avoid is captured where it is
  # enforceable: metrics.yaml's never_use (t2_pressure, t2_allocated_mb, each
  # with its reason) and tests/test_gb_semantics_rule1_overflow.py.

- id: served_ctx_structural
  kind: metric
  name: served_ctx
  source: live

- id: n_gpu_layers_effective_structural
  kind: metric
  name: n_gpu_layers_effective
  source: live

- id: cluster_vram_fill_pct_structural
  kind: metric
  name: cluster_vram_fill_pct
  source: live

- id: feeder_reachable_structural
  kind: metric
  name: feeder_reachable
  source: live

- id: gaming_session_active_structural
  kind: metric
  name: gaming_session_active
  source: live

- id: swap_pressure_structural
  kind: metric
  name: swap_pressure
  source: live

- id: iowait_pct_structural
  kind: metric
  name: iowait_pct
  source: live

- id: kmod_loaded_structural
  kind: metric
  name: kmod_loaded
  source: live

- id: kmod_loaded_version_structural
  kind: metric
  name: kmod_loaded_version
  source: live

- id: core_build_version_structural
  kind: metric
  name: core_build_version
  source: live

- id: vram_headroom_pct_structural
  kind: metric
  name: vram_headroom_pct
  source: live

- id: vram_headroom_exhausted_structural
  kind: segment
  name: vram_headroom_exhausted
  source: live

- id: shim_fresh_structural
  kind: metric
  name: shim_fresh
  source: live

- id: ssm_state_gb_structural
  kind: metric
  name: ssm_state_gb
  source: live

- id: recurrent_layer_fraction_structural
  kind: metric
  name: recurrent_layer_fraction
  source: live

- id: quant_rel_error_structural
  kind: metric
  name: quant_rel_error
  source: live

# ── segments, fixture-exact ──────────────────────────────────────────────
- id: rule1_underfilled_matches
  kind: segment
  name: rule1_underfilled
  source: fixture
  category: trap
  question: "Is Rule #1 currently violated?"
  expect: {matched: true}

- id: feeder_idle_while_host_saturated_no_match
  kind: segment
  name: feeder_idle_while_host_saturated
  source: fixture
  expect: {matched: false}
  question: "Is a feeder idle while the host is saturated?"

- id: below_quality_floor_no_match
  kind: segment
  name: below_quality_floor
  source: fixture
  expect: {matched: false}
  question: "Is the served model below the quality floor?"

- id: prompt_cache_cold_matches
  kind: segment
  name: prompt_cache_cold
  source: fixture
  expect: {matched: true}
  question: "Is the prompt cache running cold?"

- id: serve_healthy_no_match
  kind: segment
  name: serve_healthy
  source: fixture
  expect: {matched: false}
  question: "Is everything healthy right now?"

# ── segments, live (structural) ───────────────────────────────────────────
- id: weights_on_t3_structural
  kind: segment
  name: weights_on_t3
  source: live

# Structural, not fixture-exact: this segment reads real files on the box it
# runs on (/etc/greenboost/build_info, /sys/module/greenboost/version), so its
# verdict is legitimately true, false, or None depending on the machine. What
# must hold everywhere is that it answers in the right SHAPE and never
# fabricates "no drift" out of a missing file.
- id: kmod_version_drift_structural
  kind: segment
  name: kmod_version_drift
  source: live

- id: feeder_unreachable_structural
  kind: segment
  name: feeder_unreachable
  source: live

- id: gaming_inference_contention_structural
  kind: segment
  name: gaming_inference_contention
  source: live

- id: swap_thrash_not_gpu_throttle_structural
  kind: segment
  name: swap_thrash_not_gpu_throttle
  source: live

- id: kmod_missing_silent_degrade_structural
  kind: segment
  name: kmod_missing_silent_degrade
  source: live

# ── aliases ────────────────────────────────────────────────────────────
- id: alias_vram_used_pct
  kind: metric
  name: vram_used_pct
  source: fixture
  expect: {value: 42.3}
  question: "(alias check) vram_used_pct should resolve the same as vram_fill_pct"

- id: alias_tok_s
  kind: metric
  name: tok_s
  source: fixture
  expect: {value: 5.2}
  question: "(alias check) tok_s should resolve the same as tok_s_decode"

- id: alias_t2_fraction
  kind: metric
  name: t2_fraction
  source: fixture
  expect: {value: 0.72}
  question: "(alias check) t2_fraction should resolve the same as t2_pressure_fraction"

# ── error paths ───────────────────────────────────────────────────────────
- id: unknown_metric_errors_cleanly
  kind: metric
  name: totally_bogus_metric_xyz
  source: fixture
  expect: {governed: false, has_error: true}

- id: unknown_segment_errors_cleanly
  kind: segment
  name: totally_bogus_segment_xyz
  source: fixture
  expect: {has_error: true}

# ── discovery ─────────────────────────────────────────────────────────────
- id: discover_finds_vram_metric
  kind: discover
  query: "physical vram rule 1"
  expect: {contains_name: vram_fill_pct}

# ── question routing (answer()) ──────────────────────────────────────────
- id: answer_rule1_question
  kind: answer
  question: "is rule 1 satisfied?"
  source: fixture
  expect: {governed: true, intent: is_rule1_satisfied, segment_matched: {rule1_underfilled: true}}

- id: answer_prompt_cache_question
  kind: answer
  question: "why is the prompt cache hit rate so low"
  source: fixture
  expect: {governed: true, intent: prompt_cache, segment_matched: {prompt_cache_cold: true}}

- id: answer_quality_question
  kind: answer
  question: "quality floor check"
  source: fixture
  expect: {governed: true, intent: quality_and_quant, segment_matched: {below_quality_floor: false}}

- id: answer_unroutable_question
  kind: answer
  question: "what is the meaning of life"
  source: fixture
  expect: {governed: false}

- id: answer_cluster_health_question
  kind: answer
  question: "how healthy is the cluster right now"
  source: fixture
  expect: {governed: true, intent: cluster_health, segment_matched: {feeder_idle_while_host_saturated: false}}

- id: answer_kmod_question
  kind: answer
  question: "is greenboost kernel module loaded"
  source: live
  expect: {governed: true, intent: kernel_module_state}

# ── 2026-08-18 post-reboot audit additions ─────────────────────────────────
# All `source: live` — each reads the actual box (running serve session,
# nvidia-smi, NVML) rather than the frozen fixture, so the assertion is
# structural: the resolver exists, runs, and returns a well-formed dict. That
# is the regression class check_semantics_coverage.py cannot catch on its own.

- id: weights_gb_structural
  kind: metric
  name: weights_gb
  source: live

- id: pcie_link_gen_current_structural
  kind: metric
  name: pcie_link_gen_current
  source: live

- id: idle_vram_held_gb_structural
  kind: metric
  name: idle_vram_held_gb
  source: live

- id: weights_dont_fit_vram_structural
  kind: segment
  name: weights_dont_fit_vram
  source: live

- id: pcie_link_degraded_under_load_structural
  kind: segment
  name: pcie_link_degraded_under_load
  source: live

- id: idle_serve_holding_vram_structural
  kind: segment
  name: idle_serve_holding_vram
  source: live

- id: recurrent_replay_ms_structural
  kind: metric
  name: recurrent_replay_ms
  source: live

- id: prefill_dominated_by_replay_structural
  kind: segment
  name: prefill_dominated_by_replay
  source: live

- id: inference_endpoint_is_local_structural
  kind: metric
  name: inference_endpoint_is_local
  source: live

- id: cloud_inference_in_use_structural
  kind: segment
  name: cloud_inference_in_use
  source: live

# ── 2026-08-18 manifest-wipe additions ──────────────────────────────────────
# A Full Install purge resets the GB-Synapse manifest while leaving the HF
# cache untouched, so downloaded weights become unservable and the CLI dies
# with `no such model`. Happened three times on 2026-08-18. `source: live`
# because both read the real model store — the assertion is structural: the
# resolver runs and returns a well-formed dict rather than raising.

- id: unregistered_cached_models_structural
  kind: metric
  name: unregistered_cached_models
  source: live

- id: models_wiped_from_manifest_structural
  kind: segment
  name: models_wiped_from_manifest
  source: live

# ── 2026-08-18 host-OOM additions ───────────────────────────────────────────
# The OOM killer took the terminal twice this day (27.0 GB and 31.7 GB anon
# RSS). greenboost.ko's T2 OOM guard had tripped ~10 min before each kill and
# said so only in dmesg. These make that reachable through the governed layer.

- id: host_mem_available_gb_structural
  kind: metric
  name: host_mem_available_gb
  source: live

- id: t2_oom_guard_state_structural
  kind: metric
  name: t2_oom_guard_state
  source: live

- id: host_oom_imminent_structural
  kind: segment
  name: host_oom_imminent
  source: live

# ── proxy memory (2026-08-19) ────────────────────────────────────────────
# These read the ACTUAL box (is a gb_synapse_api process alive, and what is
# its VmRSS against its RLIMIT_AS), so they are structural `live` entries:
# the fixture cannot pin a process that may or may not be running. The
# behaviour worth pinning is that all three degrade to "unknown" rather than
# to "fine" when the proxy is absent , that is asserted in
# tests/test_semantics_proxy_memory.py, where it can be made deterministic.
- id: proxy_rss_mb_structural
  kind: metric
  name: proxy_rss_mb
  source: live

- id: proxy_mem_headroom_pct_structural
  kind: metric
  name: proxy_mem_headroom_pct
  source: live

- id: proxy_memory_near_cap_structural
  kind: segment
  name: proxy_memory_near_cap
  source: live
  question: "Is the gb-synapse proxy about to run out of memory?"

# ── agent-evolution kinds (AE-*, 2026-08-19) ─────────────────────────────────
# Structural `live` entries: these read the real dataflux log, whose agent_*
# content depends on whether a benchmark or compaction has run on this box.
# The behaviour worth pinning , that all of them degrade to "unknown" rather
# than to a reassuring number when no event exists , is asserted
# deterministically in tests/test_semantics_agent_metrics.py.
- id: agent_eval_overall_structural
  kind: metric
  name: agent_eval_overall
  source: live

- id: agent_hallucinated_tool_pct_structural
  kind: metric
  name: agent_hallucinated_tool_pct
  source: live

- id: agent_compaction_prefix_kept_pct_structural
  kind: metric
  name: agent_compaction_prefix_kept_pct
  source: live

- id: agent_memory_recall_chars_structural
  kind: metric
  name: agent_memory_recall_chars
  source: live

- id: agent_compaction_broke_prefix_structural
  kind: segment
  name: agent_compaction_broke_prefix
  source: live
  question: "Did compaction throw away the prompt cache?"

- id: agent_hallucinating_tool_names_structural
  kind: segment
  name: agent_hallucinating_tool_names
  source: live
  question: "Is the model making up tool names?"

- id: agent_eval_below_reference_structural
  kind: segment
  name: agent_eval_below_reference
  source: live
  question: "Is the agent harness underperforming for this model size?"
